pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            input {
                message "Name?"
                parameters {
                    string(name: 'NAME')
                }
            }
            input {
                message "Age?"
                parameters {
                    string(name: 'AGE')
                }
            }
            steps {
                echo "$NAME"
                echo "$AGE"
            }
        }
    }
}
