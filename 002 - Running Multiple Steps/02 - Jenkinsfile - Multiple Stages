def writeToFile(text='') {
	sh "date > $text && cat $text"
}


pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
    	stage('build') {
		writeToFile('file.txt')
	}
	stage('test') {
		sh "cat file.txt | grep 2022"
	}
    }
}
